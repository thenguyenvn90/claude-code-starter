{
  "_comment": "Merge this into ~/.claude/settings.json (or .claude/settings.json). Adjust the command path to where you put protect-env.sh.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Bash|Grep",
        "hooks": [
          { "type": "command", "command": "~/.claude/hooks/protect-env.sh" }
        ]
      }
    ]
  }
}
